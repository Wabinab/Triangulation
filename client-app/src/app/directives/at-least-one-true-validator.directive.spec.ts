import { AtLeastOneTrueValidatorDirective } from './at-least-one-true-validator.directive';

describe('AtLeastOneTrueValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AtLeastOneTrueValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
