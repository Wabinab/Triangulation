import { NumberNodotValidatorDirective } from './number-nodot-validator.directive';

describe('NumberNodotValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberNodotValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
