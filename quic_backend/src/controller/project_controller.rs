use crate::*;

// ===========================================

pub(crate) fn new_project(data_path: PathBuf, msg: Bytes) -> Result<Option<String>, String> {
  
  // Check for version will be done later, in another 'helper' file. 

  Ok(Some("".to_owned()))
}